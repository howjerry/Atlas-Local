import java.util.*;

public class Pass {
    public void process() {
        List<String> items = new ArrayList<>();
        Map<String, Integer> data = new HashMap<>();
        Set<Long> unique = new HashSet<>();
    }
}
