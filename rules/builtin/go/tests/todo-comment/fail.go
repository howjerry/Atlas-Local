package main

// TODO: refactor this function to use io.Reader
func processInput(data string) string {
	return data
}

// FIXME: handle edge case when input is empty
func validate(input string) bool {
	return len(input) > 0
}

// HACK: temporary workaround for upstream bug
func workaround() int {
	return 42
}
